import { Tool } from "@modelcontextprotocol/sdk/types";

export const commonIdDescription =
  "It should be a 32-character string (excluding hyphens) formatted as 8-4-4-4-12 with hyphens (-).";

export type NotionTool = Tool;
